import ProductsList from '../components/ProductsList';

const ProductsPage = () => {
  return (
    <main className="px-8 py-8">
      <ProductsList />
    </main>
  );
};

export default ProductsPage;
